# -*- coding: utf-8 -*-
"""
Auto-generated _vendor package init.
Makes this directory importable and adds it to sys.path.
"""
import sys
from pathlib import Path

_VENDOR_DIR = Path(__file__).resolve().parent
_VENDOR_DIR_STR = str(_VENDOR_DIR)

if _VENDOR_DIR_STR not in sys.path:
    sys.path.insert(0, _VENDOR_DIR_STR)
